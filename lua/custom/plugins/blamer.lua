return {
  'APZelos/blamer.nvim',
  -- You might need to add configuration options or keymaps here
  -- For example, to enable it on startup:
  lazy = false,
  -- Or if you want it to lazy-load based on a keymap or command:
  -- event = "BufReadPost", -- or a specific command, e.g., cmd = "BlamerToggle"
  -- keys = {
  --   { "<leader>gb", "<cmd>BlamerToggle<CR>", desc = "Toggle Blamer" },
  -- },
}
